# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
.cursor/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VS Code / Cursor
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/
.metadata

# Platform build artifacts
ios/Flutter/ephemeral/
ios/Pods/
ios/.symlinks/
android/.gradle/
android/app/build/
macos/Flutter/ephemeral/
macos/Pods/

# Generated plugin registrants
lib/generated_plugin_registrant.dart
macos/Flutter/GeneratedPluginRegistrant.swift

# Not supported build targets for this app -- only Android, iOS, and macOS
# (kept for local desktop dev/testing) are. These were added by `flutter
# create .` regenerating every platform target; excluded rather than
# deleted in case that changes later. Run `flutter create .
# --platforms=windows` (etc.) to regenerate if ever needed.
/windows/
/linux/
/web/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Exceptions to above rules.
!/packages/flutter_tools/test/data/dart_dependencies_test/**/.packages
